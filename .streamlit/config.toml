[browser]
gatherUsageStats = false

[server]
# Cap uploads (MB) for audio/images in this app; raise if you need longer recordings.
maxUploadSize = 50

[theme]
base = "dark"
# Muted, dark-first defaults (overridden by CSS accents in app.py)
primaryColor = "#7C3AED"
backgroundColor = "#0B0F1A"
secondaryBackgroundColor = "#121827"
textColor = "#E6EAF2"
font = "sans serif"
[client]
